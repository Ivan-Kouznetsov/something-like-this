{
  "name": "something-like-this",
  "version": "0.1.0",
  "description": "something-like-this",
  "main": "index.js",
  "author": {
    "name": "Ivan Kouznetsov"
  },
  "devDependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "jest": "^26.1.0",
    "prettier": "2.0.5"
  },
  "dependencies": {
    "jsonpath": "^1.0.2",
    "node-fetch": "^2.6.0"
  },
  "scripts": {
    "test": "echo Make sure the test server is running && jest",
    "test-cov": "jest --coverage",
    "prettierfix": "prettier --print-width 120 --write ."
  }
}
